**Switch** — toggle a boolean. Used for detector enable/disable, density mode, notification channels.

```jsx
<Switch checked={enabled} onChange={setEnabled} label="Detector enabled" />
<Switch checked={dense} onChange={setDense} />
```

Ink-filled track when on. Pair with a label or drive from a settings row.
